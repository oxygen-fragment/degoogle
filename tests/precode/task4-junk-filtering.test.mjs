import test from "node:test";
import assert from "node:assert/strict";

test("Task 4 precode: help should document which junk domains are filtered by default", () => {
  // TODO(Task 4): replace this placeholder with a real assertion against --help output.
  // Expected future behavior:
  // - help clearly states default junk filtering behavior
  // - help includes representative domains (youtube, quora, facebook, pinterest)
  assert.fail("Precode anchor: Task 4 not implemented yet.");
});
